# Runtime dependencies.
#
# Tested against CPython 3.11-3.13. Python 3.14 is not recommended yet:
# opencv-python does not publish wheels for it at these versions, and pip
# will try (and usually fail) to build OpenCV from source.
#
# Install PyTorch FIRST, from the index that matches the GPU -- PyPI's
# Windows wheels are CPU-only. CUDA 12.8 covers RTX 50-series (Blackwell):
#
#   pip install torch==2.11.0 torchvision==0.26.0 --index-url https://download.pytorch.org/whl/cu128
#
# System packages are NOT installable from here -- see README.md for the
# poppler-utils prerequisite.

fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# --- Surya (the OCR engine) ---
# 0.17.x is the last in-process release; 0.20+ needs a separate vLLM or
# llama.cpp server and returns block-level text without word boxes.
surya-ocr==0.17.1
torch==2.11.0
torchvision==0.26.0
# surya-ocr 0.17.1 declares transformers>=4.56.1 but breaks on 5.x
# (SuryaDecoderConfig has no pad_token_id).
transformers==4.57.6
# Imported by surya.common.s3 for the model download but not declared.
requests==2.34.2

# surya-ocr pins pillow<11 and this exact OpenCV build.
pillow==10.4.0
# Headless build: same `cv2` module, minus the GUI/X11 stack that a server
# image has no use for.
opencv-python-headless==4.11.0.86
numpy==2.1.3

pdf2image==1.17.0

# Best-effort language identification of the extracted text. Neither engine
# reports which language it actually read.
langdetect==1.0.9

pydantic==2.10.4

# --- Saved lab reports (MySQL / MariaDB) ---
PyMySQL==1.2.0
python-dotenv==1.2.3
